kernel_x64:
  active: true
  subdir: kernel/${KERNEL.DEFAULT.RELEASE}/x64
  sources:
    - type: git
      url: ${KERNEL.DEFAULT.SOURCE.GITHUB.URL}
      branch: ${KERNEL.DEFAULT.SOURCE.GITHUB.BRANCH}
      depth: 1
      subdir: "."
  builders:
    - type: make.kernel
      subdirs:
        target: "."
        product: "."
        deploy: "."
      defconfig: ${KERNEL.DEFAULT.CONFIG.X64}
      arch: "x86_64"
      compiler: "x86_64-linux-gnu-"
      jobs: 4
      env:
        - ""
      artifacts:
        - "vmlinuz-6.11.0"
        - "lib"
      deps:
        - ""

kernel_aarch64:
  active: true
  subdir: kernel/${KERNEL.DEFAULT.RELEASE}/aarch64
  sources:
    - type: git
      url: ${KERNEL.DEFAULT.SOURCE.GITHUB.URL}
      branch: ${KERNEL.DEFAULT.SOURCE.GITHUB.BRANCH}
      depth: 1
      subdir: "."
  builders:
    - type: make.kernel
      subdirs:
        target: "."
        product: "."
        deploy: "."
      defconfig: ${KERNEL.DEFAULT.CONFIG.ARM64}
      arch: "arm64"
      compiler: "aarch64-linux-gnu-"
      jobs: 4
      env:
        - ""
      artifacts:
        - "vmlinuz-6.11.0"
        - "lib"
      deps:
        - ""

