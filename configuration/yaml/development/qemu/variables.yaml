QEMU: 
  MASTER: &QEMU_MASTER_ALIAS
    RELEASE: "master"
    SOURCE:
      GITHUB:
        URL: "https://gitlab.com/qemu-project/qemu.git"
        BRANCH: ${QEMU.MASTER.RELEASE}

  STABLE_9_1: &QEMU_STABLE_9_1_ALIAS
    RELEASE: "stable-9.1"
    SOURCE:
      GITHUB:
        URL: "https://gitlab.com/qemu-project/qemu.git"
        BRANCH: ${QEMU.STABLE_9_1.RELEASE}

  DEFAULT: *QEMU_MASTER_ALIAS
